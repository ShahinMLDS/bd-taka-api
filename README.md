# bd-taka-api

A simple API project for Bangladeshi taka data.

## Requirements

- Python 3.10+
- Flask (or whichever framework your app uses)

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
